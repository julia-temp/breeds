import { MissingImageDirective } from './missing-image.directive';

describe('MissingImageDirective', () => {
  it('should create an instance', () => {
    const directive = new MissingImageDirective();
    expect(directive).toBeTruthy();
  });
});
